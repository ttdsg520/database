<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jack.db.mapper.RejectionRecordMapper">

    <insert id="insertRejectionRecord">
        INSERT INTO rejection_record
        (review_exam_form_id, student_name, student_phone, rejection_reason, reason, created_at)
        VALUES (#{reviewExamFormId}, #{studentName}, #{studentPhone}, #{rejectionReason}, #{reason}, #{createdAt})
    </insert>
</mapper>
